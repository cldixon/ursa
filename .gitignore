# Rust
/target
**/*.rs.bk
# Cargo.lock IS committed: this workspace produces a distributable wheel, so we
# pin the exact arrow/datafusion/pyo3 resolution for reproducible builds.

# Python / maturin
__pycache__/
*.py[cod]
.venv/
/dist
/wheelhouse
*.so
*.pyd
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Docs site (Astro). package-lock.json IS committed so the Pages build is
# reproducible and `npm ci` has something to resolve against.
/site/node_modules/
/site/dist/
/site/.astro/
/site/.wrangler/

# Editors / OS
.DS_Store
.idea/
.vscode/
